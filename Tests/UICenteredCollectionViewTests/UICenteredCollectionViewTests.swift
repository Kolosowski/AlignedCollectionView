import XCTest
@testable import UICenteredCollectionView

final class UICenteredCollectionViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UICenteredCollectionView().text, "Hello, World!")
    }
}
